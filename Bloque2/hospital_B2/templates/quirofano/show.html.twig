{% extends 'base.html.twig' %}

{% block title %}Quirofano{% endblock %}

{% block body %}
    <h1>Quirofano</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ quirofano.id }}</td>
        </tr>
        <tr>
            <th>CodigoQuirofano</th>
            <td>{{ quirofano.codigoQuirofano }}</td>
        </tr>
        <tr>
            <th>Estado</th>
            <td>{{ quirofano.estado ? 'Libre' : 'Ocupado' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quirofano_index') }}">back to list</a>

    <a href="{{ path('app_quirofano_edit', {'id': quirofano.id}) }}">edit</a>

    {{ include('quirofano/_delete_form.html.twig') }}
{% endblock %}
