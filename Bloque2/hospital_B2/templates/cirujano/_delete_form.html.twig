<form method="post" action="{{ path('app_cirujano_delete', {'id': cirujano.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cirujano.id) }}">
    <button class="btn">Delete</button>
</form>
