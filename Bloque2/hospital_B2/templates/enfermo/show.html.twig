{% extends 'base.html.twig' %}

{% block title %}Enfermo{% endblock %}

{% block body %}
    <h1>Enfermo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enfermo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ enfermo.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ enfermo.apellidos }}</td>
            </tr>
            <tr>
                <th>Enfermedad</th>
                <td>{{ enfermo.enfermedad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enfermo_index') }}">back to list</a>

    <a href="{{ path('app_enfermo_edit', {'id': enfermo.id}) }}">edit</a>

    {{ include('enfermo/_delete_form.html.twig') }}
{% endblock %}
