<form method="post" action="{{ path('app_enfermo_delete', {'id': enfermo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfermo.id) }}">
    <button class="btn">Delete</button>
</form>
